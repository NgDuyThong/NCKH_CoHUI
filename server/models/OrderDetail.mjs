/**
 * ES Module wrapper for OrderDetail model
 */

import { createRequire } from 'module';
const require = createRequire(import.meta.url);

const OrderDetail = require('./OrderDetail.js');

export default OrderDetail;
